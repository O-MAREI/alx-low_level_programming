#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints lowercase alphabet in reverse.
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; ch--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}

