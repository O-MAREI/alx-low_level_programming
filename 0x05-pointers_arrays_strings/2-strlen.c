#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Returns the legnth of a string
 *
 * @s: The string
 *
 * Return: The length of s
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
